1.顶部“用户”两个字不应该写死，要展示当前用户的类型
2.用户名"Shao,Blade"，改成LastName+FirstName;
3.页面顶部分头像的位置，去掉click事件，去掉数字“32”
4.去掉菜单的最顶端的“头像+人名”
5.页面上的刷新图标，加上强制刷新的功能
6.右上角的退出按钮，加上退出功能
7.有些页面重置功能没实现，或者实现不完整（需求分配，需求调整，运单查询维护）


todo：

table columns 全选，全不选
	table任何列都不折行
	table表格文字对齐

24小时
red error message让用户关闭
部分页面没有绑定userid，而是写死10000
画面中的一些增强
417message需要陈俊提供返回的格式
事件处理太慢，需要加loading效果
